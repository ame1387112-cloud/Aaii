services:
  - type: web
    name: telegram-bot
    env: python
    pythonVersion: "3.11"
    buildCommand: |
      pip install -r requirements.txt
      playwright install
      playwright install-deps
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
